<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dietplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dietplan">
        <activity android:name=".Dietplan2000Activity"></activity>
        <activity android:name=".Dietplan1800Activity" />
        <activity android:name=".Dietplan1500Activity" />
        <activity android:name=".Dietplan1200Activity" />
        <activity android:name=".HomeFragment" />
        <activity android:name=".BMRFragment" />
        <activity android:name=".BMIFragment" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>